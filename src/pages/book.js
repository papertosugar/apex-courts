/* ── Book page entry point ──
   booking.js is loaded as a plain <script> in book.html for onclick compat.
   We only import utils + supabase here to set window globals.
── */
import '../utils.js';
import '../supabase-client.js';
import '../auto-logout.js';
